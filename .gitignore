# --- Generated by `make` (all regenerate from source; never commit) ---
# vmlinux.h: bpftool btf dump file /sys/kernel/btf/vmlinux format c > vmlinux.h
vmlinux.h
# BPF ELF object (clang -target bpf) and the CO-RE skeleton (bpftool gen skeleton)
*.bpf.o
*.skel.h

# Compiled libbpf loader binaries (ch7+) — no extension, so listed by name
07-libbpf/execsnoop
08-argv-libbpf/execsnoop
09-openat/opensnoop
10-openat-ret/opensnoop
11-connect/netsnoop
12-tcpstates/tcpstates
13-tcplife/tcplife
14-verifier/verifier
15-bashreadline/bashreadline
16-bpftool/opencount
17-xdp/xdpcount
18-tc/tcstat
19-tailcall/tailcall
20-lsm/execguard
21-firewall/xdpfw
22-iterator/taskdump
23-ids/ids

# --- Ch25 fileless lab: built inside the container, never on the host ---
# Both source trees (src/ and src-v1.1-verbatim/) build the same 7 targets.
25-fileless-lab/*/hello
25-fileless-lab/*/sleeper
25-fileless-lab/*/memfd_exec
25-fileless-lab/*/memfd_execveat
25-fileless-lab/*/memfd_script
25-fileless-lab/*/tmpfile_exec
25-fileless-lab/*/deleted_exec

# --- Python (BCC chapters 1–6) ---
__pycache__/
*.pyc

# --- Machine-local Claude Code settings ---
.claude/settings.local.json
CLAUDE.md

# --- Editor / OS cruft ---
*.swp
*~
.DS_Store

# --- Ch25: the restricted revisions of the source document are NOT published
# in this public repo. v1.1 and v1.2 are TLP:GREEN with an author need-to-know
# restriction; they are distributed separately. v1.3 is TLP:CLEAR and is
# published here, so it is deliberately not matched below.
# Matched anywhere in the tree (a v1.1 PDF has sat in the repo root before),
# and in both formats. v1.3 is deliberately not matched.
*v1.1.html
*v1.1.pdf
*v1.2.html
*v1.2.pdf
